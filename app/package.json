{
  "name": "shinhan-pos-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shinhan Soft POS API - Cloudflare Workers",
  "scripts": {
    "dev": "react-router dev",
    "build": "react-router build",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 migrations apply shinhan-pos-db --local && wrangler d1 execute shinhan-pos-db --local --file=seeds/seed.sql",
    "db:init:remote": "wrangler d1 migrations apply shinhan-pos-db --remote && wrangler d1 execute shinhan-pos-db --remote --file=seeds/seed.sql",
    "db:reset": "wrangler d1 execute shinhan-pos-db --local --file=migrations/0001_initial.sql && wrangler d1 execute shinhan-pos-db --local --file=seeds/seed.sql",
    "db:reset:remote": "wrangler d1 execute shinhan-pos-db --remote --file=migrations/0001_initial.sql && wrangler d1 execute shinhan-pos-db --remote --file=seeds/seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250415.0",
    "@react-router/dev": "^7.14.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/vite-plugin": "^1.32.1",
    "@hono/react-renderer": "^1.0.1",
    "hono": "^4.12.12",
    "isbot": "^5.1.38",
    "lucide-react": "^1.8.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-router": "^7.14.0",
    "recharts": "^3.8.1"
  }
}
